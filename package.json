{
	"name": "vscode-pc-viewer",
	"displayName": "vscode-pc-viewer",
	"description": "Display points of a file.",
	"version": "0.3.0",
	"publisher": "obarads",
	"engines": {
		"vscode": "^1.44.0"
	},
	"categories": [
		"Other"
	],
	"keywords": [
		"point cloud",
		"viewer"
	],
	"icon": "logo.png",
	"repository": {
		"type": "git",
		"url": "https://github.com/Obarads/vscode-pc-viewer.git"
	},
	"activationEvents": [
		"onCustomEditor:pcviewer.pcd.viewer",
		"onCustomEditor:pcviewer.ply.viewer"
	],
	"main": "./out/extension.js",
	"contributes": {
		"customEditors": [
			{
				"viewType": "pcviewer.pcd.viewer",
				"displayName": "pcd viewer",
				"selector": [
					{
						"filenamePattern": "*.pcd"
					}
				]
			},
			{
				"viewType": "pcviewer.ply.viewer",
				"displayName": "ply viewer",
				"selector": [
					{
						"filenamePattern": "*.ply"
					}
				]
			}
		],
		"configuration": {
			"title": "pcviewer for vscode",
			"properties": {
				"pcviewer.pointMaxSize": {
					"type": "number",
					"default": 2,
					"description": "Set point max size for gui."
				},
				"pcviewer.pointDefaultSize": {
					"type": "number",
					"default": 0.05,
					"description": "Set point default size."
				},
				"pcviewer.useGridHelper": {
					"type": "boolean",
					"default": true,
					"description": "use gridhelper."
				},
				"pcviewer.bgColor": {
					"type": "string",
					"default": "#0b1447",
					"description": "Set background color."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"lint": "eslint src --ext ts",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/vscode": "^1.46.0",
		"@types/glob": "^7.1.2",
		"@types/mocha": "^7.0.2",
		"@types/node": "^14.0.13",
		"eslint": "^7.2.0",
		"@typescript-eslint/parser": "^3.2.0",
		"@typescript-eslint/eslint-plugin": "^3.2.0",
		"glob": "^7.1.6",
		"mocha": "^8.0.1",
		"typescript": "^3.9.5",
		"vscode-test": "^1.4.0"
	}
}
